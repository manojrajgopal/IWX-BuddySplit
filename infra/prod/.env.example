# Public values consumed at BUILD time by the Next.js Dockerfile.
# (These get baked into the JS bundle, so they must be set before `docker compose build`.)
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_WS_URL=https://api.example.com
